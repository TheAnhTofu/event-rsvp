"use client";

import { EventSiteFooter } from "@/components/layout/event-site-footer";

/** @deprecated Prefer importing `EventSiteFooter` directly; kept for call sites. */
export function SiteFooter() {
  return <EventSiteFooter />;
}
